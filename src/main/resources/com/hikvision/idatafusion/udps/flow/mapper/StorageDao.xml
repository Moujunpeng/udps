<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hikvision.idatafusion.udps.flow.mapper.StorageDao">

    <insert id="insertStorage" parameterType="com.hikvision.idatafusion.udps.flow.model.StorageLocal">
        insert into local_preview
        (id,data)
        values
        (#{id},#{count}
        )
    </insert>


</mapper>